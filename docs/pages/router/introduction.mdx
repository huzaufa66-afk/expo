import { View, Text } from "react-native";

export default function HomeScreen() {
  return (
    <View style={{
      flex: 1,
      justifyContent: "center",
      alignItems: "center"
    }}>
      <Text style={{fontSize: 30, fontWeight: "bold"}}>
        Barde Delivery 24
      </Text>

      <Text style={{fontSize: 18}}>
        Sannu, ka shigo app dinmu
      </Text>
    </View>
  );
}
